name: data_vault
description: "DataVault — Secure File Sharing & Access Analytics Platform"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.4.0
  cloud_firestore: ^6.1.2
  firebase_auth: ^6.1.4
  firebase_storage: ^13.0.6
  
  # Added for Hackathon Strategy
  file_picker: ^8.1.7
  fl_chart: ^0.70.2
  intl: ^0.20.2
  share_plus: ^10.1.4
  path: ^1.9.1
  provider: ^6.1.5+1
  url_launcher: ^6.3.2
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
